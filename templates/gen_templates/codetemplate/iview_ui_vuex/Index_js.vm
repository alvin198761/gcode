import Vue from 'vue'
import Vuex from 'vuex'
import app from './app';
import  ${upp}  from '../pages/${cond.model}/${low}/${upp}';
Vue.use(Vuex)
export default new Vuex.Store({
  modules: {
    app:app,
    ${lowUpp}: ${upp},
  }
})

