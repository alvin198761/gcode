<assembly xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.0 http://maven.apache.org/xsd/assembly-1.1.0.xsd">
    <id>bin</id>
    <baseDirectory>${project.name}</baseDirectory>
    <formats>
        <format>zip</format>
    </formats>
    <files>
        <file>
            <source>${dollar}{project.build.directory}/${dollar}{project.build.finalName}.jar</source>
            <outputDirectory>./</outputDirectory>
            <destName>${project.name}.jar</destName>
        </file>
    </files>
    <fileSets>
        <fileSet>
            <directory>${dollar}{basedir}/src/main/resources</directory>
            <outputDirectory>./</outputDirectory>
            <includes>
                <include>/application.yml</include>
            </includes>
        </fileSet>
        <fileSet>
            <directory>${dollar}{basedir}/src/main/resources/config</directory>
            <outputDirectory>./config</outputDirectory>
            <includes>
                <include>/application-prod.yml</include>
            </includes>
        </fileSet>
    </fileSets>
</assembly>